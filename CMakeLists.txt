list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")

# TODO - chech if already included from installation

include(SetupUtils)
include(openDAQUtils)
include(DependencyUtils)
include(Resources)
include(BoostUtils)
include(ModuleUtils)
